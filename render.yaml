services:
  - type: web
    name: classroom-tracker
    runtime: node
    buildCommand: npm install && npm run seed
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
